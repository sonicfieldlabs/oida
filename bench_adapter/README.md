# Bench Adapter

The Python package lives in `bench_adapter`; this directory preserves the plan's
`bench-adapter/` shape. It builds a small `DirectBenchmarkRunPayload`-style
object for the AKOUO benchmark API without modifying AKOUO.

