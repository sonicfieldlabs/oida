# Oída for OpenCode

`oida integrate opencode` installs the Oída listening skill globally and adds
an idempotent local stdio MCP entry to OpenCode's JSON configuration. Existing
provider authentication remains owned by OpenCode.
