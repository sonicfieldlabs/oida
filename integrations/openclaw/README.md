# Oída for OpenClaw

This Claude-compatible local marketplace contributes the Oída listening skill,
command, and stdio MCP gateway. OpenClaw continues to own its configured models
and provider authentication. Install it with `oida integrate openclaw`.
